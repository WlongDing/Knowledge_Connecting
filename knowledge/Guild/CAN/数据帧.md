



![[Pasted image 20260707214254.png]]

## 标准帧
|Bus idle|SOF|ID|RTR|IDE|r0|
DLC|Data Field|CRC|DEL|ACK|DEL|EOF|ITM|Bus idle|

SOF：帧起始 -- “0”
ID：11位，  禁止高7位都为隐性，即禁止设定：ID=1111111XXXX
RTR：“1”表示远程帧，“0”表示数据帧
IDE：“0”表示为标准帧
r0：保留位 为“0” 
DLC：表示数据长度，0~8，表示0~8个字节。9~15，也表示为8个字节 
## 扩展帧
|Bus idle|SOF|ID|SRR||IDE|Extended ID|RTR|r1|r0|
DLC|Data filed|CRC|DEL|ACK|DEL|EOF|ITM|Bus idle|
 
SOF：帧起始 -- “0”
ID：29位
SRR：无意义 为“1”
IDE：“1”表示为扩展帧
r1、r0：保留位 为“0”

CRC：15位 循环冗余校验
CRC DEL：隐性位 固定为 “1”
ACK ：前面数据  对的话为“0”， 错的话为“1” -- 发送节点在ACK段发送2个位的隐性位
ACK DEL：隐性位 固定为 “1”

EOF：固定7个“1”
ITM：固定3个“1”

所以ACK DEL+EOF+ITM等于11个连续的隐性位，那么表示总线空闲了。

在CRC DEL(界定符)之前进行位填充，之后是固定格式，不允许位填充 。


## 远程帧

如果是远程帧的话那么是没有数据的，即DLC为0，只有一个ID。RTR为“1”

是用来索取数据的。

![[Pasted image 20260709214130.png]]

7优先级法则
​ 数据帧和遥控帧的仲裁段用于多个节点竞争总线时进行仲裁，优先级高的帧获得再总线上发送数据的权利。优先级的确认总结为以下几条法则：

- 在总线空闲时，最先开始发送消息的节点获得发送权；
- 多个节点同时开始发送时，从仲裁段的第一位开始进行仲裁，第一次出现各节点的位电平互异时，输出显性电平的节点获得发送权；
- 相同ID和格式的数据帧和遥控帧，数据帧具有更高优先级，因为数据中的RTR位时显性电平，而遥控帧的RTR位是隐性电平；
- 对于11位标准ID相同的标志数据帧和扩展数据帧，标准数据帧具有更高的优先级，因为标志数据帧的IDE位是显性电平，而扩展数据帧的IDE位是隐性电平。